# Project rules

- When making changes, always update README.md to reflect the current behavior
- Always bump the version in both `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` before committing a release
- Use semantic versioning: patch for fixes, minor for features, major for breaking changes
